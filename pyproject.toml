[project]
name = "govuk-watcher"
version = "0.0.0"
description = "Prometheus metrics exporter for anything"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "logfire>=4.7.0",
    "prometheus-client>=0.22.1",
    "pydantic>=2.11.9",
    "requests>=2.32.5",
]
